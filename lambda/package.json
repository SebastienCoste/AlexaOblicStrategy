{
  "author": "scoste",
  "name": "oblic-strategy-skill",
  "version": "1.0.0",
  "description": "Stuck ? Try the oblic strategy",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.20"
  }
}
